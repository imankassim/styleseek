# discarded/

Clean, rejected experiments kept for the evidence trail — never deleted. One folder per rejected
`EXP-ID`, each with its code/config and a short written reason it was rejected. See the
negative-result policy in `docs/architecture/STYLESEEK_ARCHITECTURE.md` §14 and
`experiments/experiment_register.md`.
