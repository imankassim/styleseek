# tests/accessibility/
axe-core scan of the running frontend (idle, browse, basket, post-search results) — architecture §15 "Accessibility". Manual/occasional, not wired into CI (needs a real browser download) — see `check_axe.js`'s header comment for how to run it.

Last run (2026-09-08): zero violations across all four states.
